import type { Props } from "@/types/Props";
import { NucoWrapper } from "@/components/wrapper/NucoWrapper";
import { {{ pascalCase name }} as Class, type {{ pascalCase name }}Type, {{ pascalCase name }}Util } from "@nuco/core/components/{{ name }}";

// Please change the type of ElementType to the correct type
type ElementType = HTMLElement;

export const {{ pascalCase (replace name "n-" "") }} = (props: Props<ElementType, {{ pascalCase name }}Type["Props"], {{ pascalCase name }}Type["Emit"]>) => {
  const { getHtmlHast, style } = {{ pascalCase name }}Util;

  return (
    <NucoWrapper<"{{ name }}", ElementType, {{ pascalCase name }}Type["Props"], {{ pascalCase name }}Type["Emit"]>
      elementName="{{ name }}"
      elementClass={Class}
      getNElementHtml={getHtmlHast}
      style={style}
      props={props}
    />
  );
};
import type { Meta, StoryObj } from "@storybook/web-components";
import { renderElement } from "@root/.storybook/utils/renderElement";
import { {{pascalCase name}}, type {{pascalCase name}}Type } from "./{{pascalCase name}}.ce";

// This default export determines where your story goes in the story list
const meta: Meta<{{pascalCase name}}Type["Props"]> = {
  component: "{{kebabCase name}}",
  tags: ["autodocs"],
  argTypes: {},
  args: {},
  render: (attr) => {
    const elm = renderElement("{{kebabCase name}}", {{pascalCase name}}, attr);

    return elm;
  },
};

export default meta;
type Story = StoryObj<{{pascalCase name}}Type["Props"]>;

export const Primary: Story = {
  args: {},
};
